services:
  mysql:
    image: mysql:8.0
    container_name: mysql-cad-clientes
    environment:
      MYSQL_ROOT_PASSWORD: 123
      MYSQL_DATABASE: mysql-cad-clientes
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
volumes:
  mysql_data:
